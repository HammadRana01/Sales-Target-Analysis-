# Sales-Target-Analysis-
The project implements a robust ETL (Extract, Transform, Load) pipeline using Python and SQLAlchemy to migrate data from flat CSV files into a structured PostgreSQL Star Schema database. By organizing data into dedicated dimension tables (Customer, Product, Date, Region, Category) and a central Fact table, the system optimizes data integrity.
